@model IEnumerable<FinalPractice.Models.Classification>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Artwork.ArtworkTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Genre.Genre1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Artwork.ArtworkTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Genre.Genre1)
        </td>

    </tr>
}

</table>
